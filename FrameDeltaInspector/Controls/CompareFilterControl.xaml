<UserControl x:Class="FrameDeltaInspector.Controls.CompareFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FrameDeltaInspector.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="ListBoxFilters" Grid.Row="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- Wir rendern das UserControl direkt in der ListBox -->
                    <ContentPresenter Content="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <Button Padding="4" Content="Add" Click="Button_Add_Click"/>
            <Button Padding="4" x:Name="Button_Remove" Content="Remove" IsEnabled="False" Click="Button_Remove_Click"/>
            <Button Padding="4" x:Name="Button_Edit" Content="Edit" IsEnabled="False" Click="Button_Edit_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
