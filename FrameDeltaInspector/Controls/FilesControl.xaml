<UserControl x:Class="FrameDeltaInspector.Controls.FilesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FrameDeltaInspector.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Selecter Frame A -->
        <local:SequenceControl x:Name="SequenceASelectFrame" Grid.Row="0" Grid.Column="0"/>

        <!-- Selecter Frame B -->
        <local:SequenceControl x:Name="SequenceBSelectFrame" Grid.Row="1" Grid.Column="0"/>

        <local:SequencePropertyControl x:Name="sequencePropertyControl" Grid.Column="1" Grid.RowSpan="2"/>
    </Grid>
</UserControl>
